apiVersion: v1
kind: Secret
metadata:
  name: embedding-service-secret
  namespace: rag-system
  labels:
    app: embedding-service
type: Opaque
stringData:
  # Replace with actual API key in production
  # For local development, this will be overridden by .env file
  OPENAI_API_KEY: "sk-your-key-here"
