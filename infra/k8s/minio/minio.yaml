apiVersion: v1
kind: Service
metadata:
  name: xrag-minio
  namespace: rag-system
spec:
  ports:
    - port: 9000
      name: api
    - port: 9001
      name: console
  selector:
    app: xrag-minio
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: xrag-minio
  namespace: rag-system
spec:
  serviceName: xrag-minio
  replicas: 1
  selector:
    matchLabels:
      app: xrag-minio
  template:
    metadata:
      labels:
        app: xrag-minio
    spec:
      containers:
        - name: minio
          image: minio/minio:latest
          args: ["server", "/data", "--console-address", ":9001"]
          ports:
            - containerPort: 9000
            - containerPort: 9001
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: minio-credentials
                  key: root-user
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: minio-credentials
                  key: root-password
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
